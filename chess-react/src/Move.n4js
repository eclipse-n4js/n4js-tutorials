import {Coordinate} from "Coordinate";
import {Piece} from "Piece";

/**
 * This class represents a move
 */
export public class Move {
    public piece: Piece;
    public fromPos: Coordinate;
    public toPos: Coordinate;
    
    public constructor(@Spec spec:~i~this) {}
    

    @Override
    public toString(): string {
        return this.piece + ' ' + this.fromPos.getBoardCoordinateRepresentation() + '→' + this.toPos.getBoardCoordinateRepresentation();
    }
}
